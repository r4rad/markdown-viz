{
  "name": "markdown-viz",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Lightweight, performance-oriented Markdown editor, viewer & beautifier with multi-tab sessions, VS Code-like themes, and diagram support",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.3.0"
  }
}
